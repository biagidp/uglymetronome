# Framework

The Framework version of Ugly Metronome builds on the features introduced in ModernTooling by adding React. This complicates the file structure further, but introduces some conventions and organization practices that make the code easier to reuse and maintain.

- Adds React
